import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Quiz = () => {
  return (
    <View>
      <Text>Quiz</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Quiz;
